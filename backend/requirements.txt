fastapi>=0.115.0
uvicorn[standard]>=0.32.0

pydantic>=2.10.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12

sqlalchemy>=2.0.36
alembic>=1.14.0
python-dotenv==1.0.0

# Docling (CPU only)
docling==2.63.0
docling-core==2.52.0
docling-ibm-models==1.0.0

# FORCE Docling to use ONNX CPU only (disable GPU backends)
onnxruntime==1.17.0
rapidocr==3.4.2
rapidocr_onnxruntime==1.0.0

# Prevent Transformers from installing CUDA torch
transformers==4.44.2
accelerate==1.0.1
tokenizers==0.20.1

# HARD BLOCK PyTorch
torch==0.0.0  # fake version to block install
torchvision==0.0.0
torchaudio==0.0.0

# Image processing
Pillow>=10.3.0
python-magic

# DB
psycopg2-binary>=2.9.9

python-dateutil==2.8.2
pytz==2023.3

google-generativeai>=0.8.0
